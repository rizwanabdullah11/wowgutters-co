# Required for the quote form (static site = browser POSTs to Web3Forms; no API on Hostinger).
# Copy to ".env.local" for npm run dev, and ".env.production" before npm run build / Hostinger upload.
# https://web3forms.com — set your Gmail as the notification address in the dashboard.
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=6ebf3423-95f8-4120-9d52-183cb21022de
